# 构建上下文瘦身。
# 环境镜像只依赖 pyproject.toml / uv.lock；项目镜像复制源码 + **两份宿主机构建好的
# 静态成品**（frontend/dist、www），其余一律不进上下文。

# 虚拟环境与包缓存（uv 在镜像内重新安装）
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# 前端依赖（成品要进镜像，所以这里只排除依赖目录）
frontend/node_modules/
.pnpm-store/

# 运行时数据（配置仓库、上传、日志）：不进镜像，运行时用卷挂载
data/
media/
logs/
tmp/

# 注意：frontend/dist 与 www 必须留在上下文里 —— 它们是宿主机上构建好的静态成品，
# Dockerfile.app 要把它们 COPY 进镜像（nginx 通过命名卷读取）。
# 这两个目录在 .gitignore 里被忽略，但 docker build 只认本文件。

# 反向代理资源（由 nginx 容器挂载，不进应用镜像）
nginx/ssl/

# 版本控制与本地环境
.git/
.gitignore
.vscode/
.idea/
.env
.env.*
*.log
nohup.out

# 容器环境变量文件与 docker secrets（env/*.env 由 compose 在运行时注入、env/secrets/* 只挂给
# 运行中的容器，两者都不是构建期输入，绝不进镜像）
env/

# 文档与 AI 工具
docs/
.mimocode/
.django-stub-gen/
README.md
AGENTS.md

# 容器编排与容器文件自身（COPY . . 时不需要）
docker-compose.yml
docker-compose.override.yml
Dockerfile*
.dockerignore

# 跨会话知识库（软链指向 ~/.dsh/skills，与镜像无关）
.dsh/
