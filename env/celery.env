# 只给 worker 服务（celery）。
#
# celery 的参数写在 docker-compose.yml 的 command 里（worker 走的是
# `entrypoint: ["/bin/sh", "-c"]`，走不到 app 那条 gunicorn 拼装路径），这里只放
# 日志级别，由容器内的 shell 展开后传给 --loglevel。
#
# 注意 compose 会对自己文件里的 `${...}` 做变量替换，而它**只读根目录的 .env**、
# 读不到本文件，所以那里写的是 `$${CELERY_LOGLEVEL:-info}`（`$$` 转义成字面 `$`），
# 交给容器内的 shell 展开——这样才能用上本文件里的值。
#
# **`--concurrency` 刻意不暴露、在 docker-compose.yml 的 command 里写死为 2**：
# 同一台设备的配置并发入库会写坏数据（见 AGENTS.md），且默认池按 CPU 核数起子进程
# （16 核 = 每容器 16 × 81M，空载就上千 MB）。调它之前先看「合并 worker」那段注释。
CELERY_LOGLEVEL=info
